import React, { useState } from 'react';
import { auth } from '../../lib/firebase';
import { Building2, CheckCircle2, Copy, KeyRound, Loader2, ShieldCheck } from 'lucide-react';
import { useToast } from '../../contexts/ToastContext';

const AdminSchoolOnboarding: React.FC = () => {
  const { toast } = useToast();
  const [form, setForm] = useState({ name: '', contactName: '', email: '', phone: '', address: '', state: '', schoolCode: '', notes: '' });
  const [saving, setSaving] = useState(false);
  const [result, setResult] = useState<any | null>(null);

  const update = (key: string, value: string) => setForm(current => ({ ...current, [key]: value }));
  const submit = async (event: React.FormEvent) => {
    event.preventDefault();
    setSaving(true);
    try {
      const user = auth.currentUser; if (!user) throw new Error('Your administrator session has expired.');
      const token = await user.getIdToken();
      const response = await fetch('/.netlify/functions/admin-school-onboard', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify(form) });
      const data = await response.json().catch(() => ({}));
      if (!response.ok) throw new Error(data.error || 'Unable to onboard school.');
      setResult(data); toast.success(`${data.school.name} has been onboarded successfully.`);
    } catch (error) { toast.error(error instanceof Error ? error.message : 'Unable to onboard school.'); }
    finally { setSaving(false); }
  };
  const copy = async (value: string, label: string) => { await navigator.clipboard.writeText(value); toast.success(`${label} copied.`); };

  if (result) return <div className="mx-auto max-w-3xl space-y-6"><div className="flex items-start gap-4 rounded-2xl border border-emerald-200 bg-emerald-50 p-6 dark:border-emerald-900/50 dark:bg-emerald-950/30"><CheckCircle2 size={28} className="mt-0.5 shrink-0 text-emerald-600"/><div><h1 className="text-2xl font-black text-emerald-900 dark:text-emerald-200">School onboarded</h1><p className="mt-1 text-sm text-emerald-800 dark:text-emerald-300">The school record, school administrator account and onboarding audit record have been created.</p></div></div><div className="rounded-2xl border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900"><div className="grid gap-4 sm:grid-cols-2"><div><span className="text-[10px] font-black uppercase tracking-wider text-slate-400">School</span><p className="mt-1 font-black text-slate-900 dark:text-white">{result.school.name}</p></div><div><span className="text-[10px] font-black uppercase tracking-wider text-slate-400">School ID</span><p className="mt-1 font-mono text-xs text-slate-700 dark:text-slate-300">{result.school.id}</p></div><div><span className="text-[10px] font-black uppercase tracking-wider text-slate-400">School Code</span><p className="mt-1 font-mono font-bold text-brand-red">{result.school.schoolCode}</p></div><div><span className="text-[10px] font-black uppercase tracking-wider text-slate-400">Administrator</span><p className="mt-1 font-semibold text-slate-900 dark:text-white">{result.administrator.name} · {result.administrator.email}</p></div></div><div className="mt-6 rounded-xl border border-amber-200 bg-amber-50 p-4 dark:border-amber-900/50 dark:bg-amber-950/30"><div className="flex items-center gap-2 text-xs font-black uppercase tracking-wider text-amber-700 dark:text-amber-300"><KeyRound size={15}/> One-time temporary password</div><div className="mt-2 flex gap-2"><input readOnly value={result.temporaryPassword} className="min-h-11 w-full rounded-xl border border-amber-200 bg-white px-3 font-mono text-sm font-bold text-slate-900 dark:border-amber-900/50 dark:bg-slate-900 dark:text-white"/><button type="button" onClick={() => void copy(result.temporaryPassword, 'Temporary password')} className="min-h-11 min-w-11 rounded-xl border border-amber-200 bg-white text-slate-700 dark:border-amber-900/50 dark:bg-slate-900 dark:text-white"><Copy size={17} className="mx-auto"/></button></div><p className="mt-2 text-xs text-amber-800 dark:text-amber-300">Give this credential to the school administrator securely. The account is configured to require a password change at first login.</p></div></div><button type="button" onClick={() => { setResult(null); setForm({ name: '', contactName: '', email: '', phone: '', address: '', state: '', schoolCode: '', notes: '' }); }} className="min-h-11 rounded-xl bg-brand-red px-5 text-sm font-bold text-white">Onboard another school</button></div>;

  return <div className="mx-auto max-w-4xl space-y-6"><div><div className="flex items-center gap-3"><Building2 className="h-8 w-8 text-brand-red"/><div><h1 className="text-3xl font-black text-brand-slate dark:text-white">Onboard a School</h1><p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Create the school record and its dedicated School Admin portal account in one controlled workflow.</p></div></div></div><div className="flex items-start gap-3 rounded-2xl border border-blue-200 bg-blue-50 p-4 text-xs leading-6 text-blue-900 dark:border-blue-900/50 dark:bg-blue-950/30 dark:text-blue-200"><ShieldCheck size={18} className="mt-0.5 shrink-0"/><span>The administrator account is created server-side, linked directly to the new school, and forced to change its temporary password on first login. The action is also written to the audit log.</span></div><form onSubmit={submit} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="grid gap-5 md:grid-cols-2"><label><span className="mb-1.5 block text-xs font-black uppercase tracking-wider text-slate-600 dark:text-slate-300">School name *</span><input required value={form.name} onChange={e => update('name', e.target.value)} className="min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm dark:border-slate-700 dark:bg-slate-800 dark:text-white" placeholder="e.g. Bright Future Academy"/></label><label><span className="mb-1.5 block text-xs font-black uppercase tracking-wider text-slate-600 dark:text-slate-300">Administrator name *</span><input required value={form.contactName} onChange={e => update('contactName', e.target.value)} className="min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm dark:border-slate-700 dark:bg-slate-800 dark:text-white" placeholder="Principal / School Admin"/></label><label><span className="mb-1.5 block text-xs font-black uppercase tracking-wider text-slate-600 dark:text-slate-300">Administrator email *</span><input required type="email" value={form.email} onChange={e => update('email', e.target.value)} className="min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm dark:border-slate-700 dark:bg-slate-800 dark:text-white" placeholder="admin@school.com"/></label><label><span className="mb-1.5 block text-xs font-black uppercase tracking-wider text-slate-600 dark:text-slate-300">Phone</span><input value={form.phone} onChange={e => update('phone', e.target.value)} className="min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm dark:border-slate-700 dark:bg-slate-800 dark:text-white" placeholder="+234…"/></label><label><span className="mb-1.5 block text-xs font-black uppercase tracking-wider text-slate-600 dark:text-slate-300">State</span><input value={form.state} onChange={e => update('state', e.target.value)} className="min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm dark:border-slate-700 dark:bg-slate-800 dark:text-white" placeholder="Lagos"/></label><label><span className="mb-1.5 block text-xs font-black uppercase tracking-wider text-slate-600 dark:text-slate-300">Custom school code</span><input value={form.schoolCode} onChange={e => update('schoolCode', e.target.value.toUpperCase())} className="min-h-11 w-full rounded-xl border border-slate-200 px-3 font-mono text-sm dark:border-slate-700 dark:bg-slate-800 dark:text-white" placeholder="Optional — generated if blank"/></label><label className="md:col-span-2"><span className="mb-1.5 block text-xs font-black uppercase tracking-wider text-slate-600 dark:text-slate-300">Address</span><input value={form.address} onChange={e => update('address', e.target.value)} className="min-h-11 w-full rounded-xl border border-slate-200 px-3 text-sm dark:border-slate-700 dark:bg-slate-800 dark:text-white" placeholder="School address"/></label><label className="md:col-span-2"><span className="mb-1.5 block text-xs font-black uppercase tracking-wider text-slate-600 dark:text-slate-300">Internal notes</span><textarea rows={4} value={form.notes} onChange={e => update('notes', e.target.value)} className="w-full rounded-xl border border-slate-200 px-3 py-3 text-sm dark:border-slate-700 dark:bg-slate-800 dark:text-white" placeholder="Partnership notes, package details, onboarding instructions…"/></label></div><div className="mt-6 flex justify-end"><button disabled={saving} type="submit" className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-brand-red px-6 text-sm font-black text-white disabled:opacity-50">{saving ? <Loader2 size={17} className="animate-spin"/> : <Building2 size={17}/>} {saving ? 'Creating school…' : 'Onboard School'}</button></div></form></div>;
};
export default AdminSchoolOnboarding;
