import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import { db, auth } from '../../lib/firebase';
import { collection, getDocs, addDoc, serverTimestamp, query, where } from 'firebase/firestore';
import { Users, FileText, Video, Clock, CheckCircle2, ShieldCheck, ArrowRight } from 'lucide-react';
import SEO from '../../components/ui/SEO';
import { DashboardGreeting } from '../../components/portal/DashboardGreeting';

const StaffDashboard: React.FC = () => {
  const [resources, setResources] = useState<any[]>([]); const [students, setStudents] = useState<any[]>([]); const [loading, setLoading] = useState(true); const [showLinkModal, setShowLinkModal] = useState(false); const [targetStudentId, setTargetStudentId] = useState(''); const [linkTitle, setLinkTitle] = useState(''); const [linkUrl, setLinkUrl] = useState(''); const [linkPlatform, setLinkPlatform] = useState('Google Meet'); const [meetingTime, setMeetingTime] = useState(''); const [submittingLink, setSubmittingLink] = useState(false); const [successMsg, setSuccessMsg] = useState(''); const [errorMsg, setErrorMsg] = useState('');
  useEffect(() => { const fetchStaffData = async () => { setLoading(true); setErrorMsg(''); try { const currentUser = auth.currentUser; if (!currentUser) return; try { const resSnap = await getDocs(collection(db, 'staffGeneralResources')); setResources(resSnap.docs.map(d => ({ id: d.id, ...d.data() }))); } catch (e) { console.warn('staffGeneralResources error:', e); } const assignmentFields = ['tutorId', 'staffId', 'assignedTutorId', 'assignedStaffId', 'instructorId']; const studentMap = new Map<string, any>(); for (const field of assignmentFields) for (const collectionName of ['individualStudents', 'students']) { try { const snap = await getDocs(query(collection(db, collectionName), where(field, '==', currentUser.uid))); snap.forEach(d => studentMap.set(d.id, { id: d.id, ...d.data() })); } catch (e) { console.warn(`Assigned ${collectionName} query failed for ${field}:`, e); } } setStudents(Array.from(studentMap.values())); } catch (err) { console.error('Error loading staff dashboard:', err); setErrorMsg('Some workspace data could not be loaded. Please try again.'); } finally { setLoading(false); } }; fetchStaffData(); }, []);
  const handlePostLink = async (e: React.FormEvent) => { e.preventDefault(); if (!targetStudentId || !linkUrl.trim()) return; if (!students.some(s => s.id === targetStudentId)) { setErrorMsg('That student is not assigned to your workspace.'); return; } setSubmittingLink(true); setErrorMsg(''); try { await addDoc(collection(db, 'personalLinks'), { studentId: targetStudentId, title: linkTitle.trim() || 'Class Session Link', url: linkUrl.trim(), platform: linkPlatform, meetingTime: meetingTime.trim(), tutorId: auth.currentUser?.uid, tutorEmail: auth.currentUser?.email || '', createdAt: serverTimestamp() }); setSuccessMsg('Classroom link posted to the assigned student portal successfully.'); setShowLinkModal(false); setLinkTitle(''); setLinkUrl(''); setMeetingTime(''); setTimeout(() => setSuccessMsg(''), 4000); } catch (err) { console.error('Error posting link:', err); setErrorMsg('Could not publish the classroom link.'); } finally { setSubmittingLink(false); } };
  return <div className="dashboard-interface space-y-8"><SEO title="Staff & Tutor Workspace Dashboard" description="Access assigned students, curriculum documents, lesson schedules, and mentor resources." noindex={true} /><DashboardGreeting name="Faculty & Instructor Console" role="STEM Mentor" subtitle="Deliver interactive lessons, manage assigned learners, publish work, and review academic activity." />
    {successMsg && <div className="p-4 rounded-xl bg-green-50 dark:bg-green-950/30 border border-green-200 text-green-800 dark:text-green-300 text-sm flex items-center gap-3"><CheckCircle2 size={18} className="text-green-600 flex-shrink-0" />{successMsg}</div>}{errorMsg && <div className="p-4 rounded-xl bg-red-50 dark:bg-red-950/30 border border-red-200 text-red-700 dark:text-red-300 text-sm flex items-center gap-3"><ShieldCheck size={18} className="text-red-600 flex-shrink-0" />{errorMsg}</div>}
    <div className="grid grid-cols-1 md:grid-cols-2 gap-6"><div className="pro-surface p-6 rounded-2xl flex items-center gap-4"><div className="w-12 h-12 bg-blue-50 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-xl flex items-center justify-center"><Users size={24}/></div><div><p className="text-xs font-semibold text-gray-500 dark:text-slate-400 uppercase tracking-wider">Assigned Cadets</p><p className="text-3xl font-black text-gray-900 dark:text-white">{students.length}</p></div></div><div className="pro-surface p-6 rounded-2xl flex items-center gap-4"><div className="w-12 h-12 bg-amber-50 dark:bg-amber-900/30 text-amber-600 dark:text-amber-400 rounded-xl flex items-center justify-center"><FileText size={24}/></div><div><p className="text-xs font-semibold text-gray-500 dark:text-slate-400 uppercase tracking-wider">Staff Guides</p><p className="text-3xl font-black text-gray-900 dark:text-white">{resources.length}</p></div></div></div>
    <div className="pro-surface rounded-3xl p-6 md:p-8"><div className="flex items-center justify-between mb-6"><div><h2 className="text-xl font-bold text-gray-900 dark:text-white">My Assigned Cadets</h2><p className="text-xs text-gray-500 mt-1">Only students explicitly assigned to your staff account are shown.</p></div><span className="text-xs text-gray-500 dark:text-slate-400">{students.length} Assigned</span></div>{loading ? <div className="text-sm text-gray-500 py-6">Loading assigned student roster...</div> : students.length === 0 ? <div className="py-10 text-center border border-dashed border-gray-200 dark:border-slate-700 rounded-2xl"><Users className="mx-auto h-10 w-10 text-gray-300 mb-3"/><p className="text-sm font-bold text-gray-900 dark:text-white">No students assigned yet</p><p className="text-xs text-gray-500 mt-1">An administrator must assign cadets to your staff account before they appear here.</p></div> : <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">{students.map(st => <div key={st.id} className="p-4 rounded-2xl border border-gray-200/80 dark:border-white/5 bg-gray-50/50 dark:bg-slate-900/60 flex flex-col justify-between"><div><div className="flex items-center justify-between mb-2 gap-2"><h3 className="font-bold text-gray-900 dark:text-white text-sm truncate">{st.fullName || st.studentName || 'Student'}</h3><span className="text-[10px] font-mono font-bold px-2 py-0.5 rounded bg-gray-100 dark:bg-slate-800 text-gray-500 dark:text-slate-400">Credentials Protected</span></div><p className="text-xs text-gray-500 dark:text-slate-400">@{st.username || 'cadet'} • {st.email || 'No email'}</p><div className="mt-3 text-xs"><span className="text-gray-500 dark:text-slate-400 block mb-1">Track / Plan:</span><span className="font-medium text-gray-800 dark:text-gray-200">{st.plan || 'Assigned Learning Track'}</span></div>{st.schedule && <div className="mt-2 text-xs text-brand-red flex items-center gap-1"><Clock size={12}/> {st.schedule}</div>}</div><div className="mt-4 pt-3 border-t border-gray-200 dark:border-white/5 flex flex-wrap gap-3"><Link to={`/portal/staff/students/${st.id}`} className="text-xs font-bold text-brand-red hover:underline flex items-center gap-1">Open Workspace <ArrowRight size={13}/></Link><button type="button" onClick={() => { setTargetStudentId(st.id); setShowLinkModal(true); }} className="text-xs font-bold text-slate-600 dark:text-slate-300 hover:text-brand-red flex items-center gap-1"><Video size={13}/> Live Link</button></div></div>)}</div>}</div>
    <div className="pro-surface rounded-3xl p-6 md:p-8"><h2 className="text-xl font-bold text-gray-900 dark:text-white mb-4">Staff Curriculum & Teaching Documents</h2>{resources.length === 0 ? <div className="text-sm text-gray-500 py-6">No teaching documents currently uploaded.</div> : <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">{resources.map(res => <a key={res.id} href={res.url || '#'} target="_blank" rel="noreferrer" className="group border border-gray-200/80 dark:border-slate-800 rounded-xl p-5 hover:border-brand-red transition-colors bg-gray-50/50 dark:bg-slate-950 flex items-start gap-4"><div className="p-2.5 rounded-lg bg-brand-red/10 text-brand-red flex-shrink-0"><FileText size={20}/></div><div><h3 className="font-bold text-gray-900 dark:text-white text-sm mb-1 group-hover:text-brand-red transition-colors">{res.title || 'Curriculum Guide'}</h3>{res.description && <p className="text-xs text-gray-500 dark:text-gray-400 line-clamp-2">{res.description}</p>}</div></a>)}</div>}</div>
    {showLinkModal && <div className="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm flex items-center justify-center p-4"><div className="bg-white dark:bg-slate-900 rounded-2xl max-w-md w-full p-6 border border-gray-200 dark:border-slate-800 shadow-2xl"><h3 className="text-lg font-bold text-gray-900 dark:text-white mb-1">Post Live Class Link</h3><p className="text-xs text-gray-500 mb-4">Broadcast a Zoom or Meet link directly to the selected assigned student.</p><form onSubmit={handlePostLink} className="space-y-4 text-xs"><div><label className="font-bold text-gray-700 dark:text-gray-300 block mb-1">Select Cadet</label><select required value={targetStudentId} onChange={e => setTargetStudentId(e.target.value)} className="w-full px-3 py-2 rounded-lg border border-gray-300 dark:border-slate-700 bg-white dark:bg-slate-800 text-gray-900 dark:text-white text-xs"><option value="">-- Choose Student --</option>{students.map(s => <option key={s.id} value={s.id}>{s.fullName || s.studentName || s.username}</option>)}</select></div><div><label className="font-bold text-gray-700 dark:text-gray-300 block mb-1">Session Title</label><input type="text" value={linkTitle} onChange={e => setLinkTitle(e.target.value)} placeholder="e.g. Python Loops & AI Logic Live Session" className="w-full px-3 py-2 rounded-lg border border-gray-300 dark:border-slate-700 bg-transparent text-gray-900 dark:text-white text-xs"/></div><div><label className="font-bold text-gray-700 dark:text-gray-300 block mb-1">Platform</label><select value={linkPlatform} onChange={e => setLinkPlatform(e.target.value)} className="w-full px-3 py-2 rounded-lg border border-gray-300 dark:border-slate-700 bg-white dark:bg-slate-800 text-gray-900 dark:text-white text-xs"><option>Google Meet</option><option>Zoom Meeting</option><option>Microsoft Teams</option><option>Scratch Live Lab</option></select></div><div><label className="font-bold text-gray-700 dark:text-gray-300 block mb-1">Meeting URL</label><input type="url" required value={linkUrl} onChange={e => setLinkUrl(e.target.value)} placeholder="https://meet.google.com/xxx-xxxx-xxx" className="w-full px-3 py-2 rounded-lg border border-gray-300 dark:border-slate-700 bg-transparent text-gray-900 dark:text-white text-xs"/></div><div><label className="font-bold text-gray-700 dark:text-gray-300 block mb-1">Meeting Time / Date (Optional)</label><input type="text" value={meetingTime} onChange={e => setMeetingTime(e.target.value)} placeholder="e.g. Today at 4:30 PM WAT" className="w-full px-3 py-2 rounded-lg border border-gray-300 dark:border-slate-700 bg-transparent text-gray-900 dark:text-white text-xs"/></div><div className="flex items-center justify-end gap-3 pt-4 border-t border-gray-100 dark:border-slate-800"><button type="button" onClick={() => setShowLinkModal(false)} className="px-4 py-2 rounded-lg border border-gray-200 dark:border-slate-700 text-gray-600 dark:text-gray-300 text-xs font-semibold">Cancel</button><button type="submit" disabled={submittingLink} className="px-4 py-2 bg-brand-red text-white text-xs font-bold rounded-lg hover:bg-red-700 transition-colors">{submittingLink ? 'Publishing...' : 'Publish to Student Portal'}</button></div></form></div></div>}
  </div>;
};
export default StaffDashboard;
