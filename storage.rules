rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Firebase Storage is not used for CMS media; uploads are handled by Cloudinary.
    // Keep this bucket closed rather than allowing arbitrary authenticated uploads.
    match /{allPaths=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}